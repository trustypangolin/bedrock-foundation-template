# Workspace specific variables
locals {
  bedrock_demo = {
    bedrock_demo = {
      # This a place holder for default overrides for a workspace called bedrock_demo. 
      # acc_map       = "diffmap"
      # alt_contacts  = "diffcontacts"
      # base_region   = "diffregion"
      # base_role     = "diffbaserole"
      # control_tower = "diffcontroltower"
      # grafana_id    = "diffgrafana"
      # notifications = "diffnotify"
      # root_emails   = "diffemails"
      # unique_prefix = "diffprefix"
    }
  }
}
